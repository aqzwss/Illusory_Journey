<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.moonlithome.game.server.account.mapper.AccountOperationMapper">
    <insert id="registAccount">
        INSERT INTO account
        (
          account_id,
          passwd,
          true_name,
          mobile,
          email,
          create_time,
          update_time
        ) VALUES (
          #{accountId},
          #{passwd},
          #{trueName},
          #{mobile},
          #{email},
          now(),
          now()
        )
    </insert>

    <select id="getPasswdClose">
      SELECT passwd
      FROM account
      WHERE account_id = #{accountId}
    </select>


</mapper>